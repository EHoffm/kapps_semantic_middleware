# Scenario 3 — The TransferUnit factory

:::{warning}
Placeholder. This is the page that needs **screenshots** — the panel and the
station board — which is why the pictures are their own patch of fog on the map.
:::

Six processes, one broker per unit, a live topology page. Run it with:

```bash
kapps-transferunit-factory
```
