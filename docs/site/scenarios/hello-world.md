# Scenario 1 — Hello World

:::{warning}
Placeholder. At release this page renders `scenario1_hello_world.ipynb`
directly, via `myst-nb` with `nb_execution_mode = "off"` — stored outputs are
shown, because a docs runner has no GraphDB.
:::

Registers a single asset, then discovers it back out of the graph.
