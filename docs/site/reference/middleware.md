# Middleware

The entry point. Extends `aas_middleware.Middleware` with graph registration, discovery and execution.

```{eval-rst}
.. automodule:: kapps_semantic_middleware.middleware
   :members:
   :show-inheritance:
```
